<UserControl x:Class="Revon.UI.Views.ImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Revon.UI.Views"
             mc:Ignorable="d" 
             >
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Label Content="{Binding Progress}" Height="60" HorizontalAlignment="Center" />
        <Button Grid.Row="1"
            x:Name="ImportFamilyFile" Height="40" Content="Download &#38; Import Family" Width="220" />
    </Grid>
    
</UserControl>
