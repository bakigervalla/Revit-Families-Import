<UserControl x:Class="Revon.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Revon.UI.Views"
             mc:Ignorable="d" 
             >
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0 40" HorizontalAlignment="Center">
            <Label Content="File URL: " />
            <TextBox Text="{Binding FileURL, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="300" VerticalContentAlignment="Center" />
        </StackPanel>
    </Grid>
    
</UserControl>
